type Film = {
  imdb_id: string;
  budget: number;
  original_language: string;
  popularity: number;
  release_date: string;
  revenue: number;
  runtime: number;
  status: string;
  vote_average: number;
  vote_count: number;
};

export default Film;
